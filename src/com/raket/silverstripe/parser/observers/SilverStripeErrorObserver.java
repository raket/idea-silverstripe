package com.raket.silverstripe.parser.observers;

/*
public class SilverStripeErrorObserver extends SilverStripeParserObserver {
	@Override
	public void update(Observable o, Object arg) {
		PsiBuilder builder = (PsiBuilder) arg;
		IElementType token = builder.getTokenType();
		if (marking) {
			observerMarker.error(ERROR_TOKEN_MESSAGES.get(token));
			marking = false;
		}
		if (ERROR_TOKENS.contains(token)) {
			observerMarker = builder.mark();
			marking = true;
		}
	}
}*/
